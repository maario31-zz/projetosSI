//QUESTÃO 2 DA LISTA DE REPETIÇÃO
//Escrever um programa em C para mostrar todos os números inteiros entre A e B (valores inteiros, lidos do teclado) que não terminem em zero.


#include <stdio.h>
#include <stdlib.h>

main ()
{  int a, b, i;

	printf("A:");
	scanf("%d", &a);
	
	printf("B:");
	scanf("%d", &b);
	
	for(i=a+1;i<b;i++){
		if(i%10!=0){
		
		printf("%d",i);
		}
		}

	
	return (0);
	system("pause>>NULL");
}
